<!DOCTYPE>
<html>
<head>
<title>Clients Updating Page</title>
</head>
  <body>
    <div class="container">
   
    <h3>Edit client:@Model["client"].GetClient()</h3>
      <form action ='/client/@Model["client"].GetClientId()/update' method="post">
      <label for="new-client">Enter a Name:</label>
      <input id = "new-client" type="text" name="new-client" value="@Model["client"].GetClient()">
      <button type = "submit" > Update </button>
      </form>
      <h4><a href="/client">Home</a></h4>
    </div>

  </body>
</html>
